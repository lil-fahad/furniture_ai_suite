# Lighter requirements for Replit environment
# Core API dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
httpx==0.25.1
pyyaml==6.0.1

# Data processing (lighter versions)
pandas==2.1.3
numpy==1.26.2
pillow==10.1.0
tqdm==4.66.1
imagehash==4.3.1
opencv-python-headless==4.8.1.78

# Optional: Kaggle integration
kaggle==1.5.16

# Note: Heavy ML dependencies (torch, timm, albumentations) are optional
# The API will work for basic features (Alibaba search, floor plan analysis)
# For full ML training features, use the full requirements.txt

# To install ML dependencies later (optional):
# pip install torch torchvision timm==1.0.8 albumentations==1.4.8
# pip install scikit-learn matplotlib onnx onnxruntime
